false

// --- 任务要求 ---
// 上文内容为 true 前，禁止结束对话。

// 使用中文与我对话。
// 使用中文与我对话。
// 使用中文与我对话。

// 每次查看此文件前，必须sleep 5秒
// 每次查看此文件前，必须sleep 5秒
// 每次查看此文件前，必须sleep 5秒

// 如果你要和我对话，不要结束你当前的对话，直接sleep 5秒然后阅读此文件即可
// 不要 运行示例并检查反馈

// 检查反馈必须是单条命令 sleep 5 && cat /home/aik/Projects/RustroverProjects/souprune/check

// ---

// --- 示例说明 ---
// simple_gb 是一个带有关键帧和缓动的简单示例

// amproj本质上是压缩包，所以你可以用命令行工具查看其内容（不要解压）
// 先解决上面的问题，再继续下面的任务。
// 基础实例以 basic_ 开头。
// 复杂示例都以complex_开头。
// 根据复杂度，从简单到复杂依次是 complex_3, complex_2, complex_1
// 特定于 指定效果 的示例都以 fx_ 开头
// ---

// --- 运行方式 ---

// cargo run -p bevy_alight_motion --example player --features xxx -- amproj文件名

// 若使用以下 feature，则启用了单元测试：
// video-comparison ，不能配合其他feature使用。它是一个单元测试。作用是对比每一帧的渲染结果与预期结果的差异。如果范围超出阀值，则测试失败。
// 你可以使用环境变量 MAX_FRAMES=n 来限制测试的最大帧数，节省时间。

// 不许降低任何测试标准，不要修改toml文件。

// --- 当前任务 ---

// Froggit enemy definition (2 ACT options)
// 蛙吉特敌人定义 (2 个 ACT 选项)
(
    facts: {
        "froggit.name": String("{{battle/ui:ENEMY_FROGGIT}}"),
        "froggit.hp": Int(100),
        "froggit.hp_max": Int(100),
        "froggit.attack": Int(4),
        "froggit.defense": Int(5),
        "froggit.description": String("Life is difficult for this enemy."),

        // ACT options count
        "act_count": Int(2),

        // Action option display labels
        "action_labels": StringList([
            "{{battle/ui:ACT_FROGGIT_CHECK}}",
            "{{battle/ui:ACT_FROGGIT_COMPLIMENT}}",
        ]),

        // Sequence paths for each action option
        "action_sequences": StringList([
            "battle/common/narration.sequence.ron",
            "battle/common/narration.sequence.ron",
        ]),

        // Parameters for each action
        "action_params": StringList([
            "{{battle/ui:ACT_FROGGIT_CHECK_RESULT}}",
            "{{battle/ui:ACT_FROGGIT_COMPLIMENT_RESULT}}",
        ]),
    },
    rules: [],
)

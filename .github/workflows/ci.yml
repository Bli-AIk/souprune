name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always
  APP_NAME: souprune

permissions:
  checks: write
  contents: read

jobs:
  test:
    name: Test
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev \
            libudev-dev \
            libwayland-dev \
            libxkbcommon-dev \
            libxrandr-dev \
            libx11-dev \
            libxi-dev \
            libxinerama-dev \
            libxcursor-dev \
            libgl1-mesa-dev \
            pkg-config \
            mold \
            jq

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "ci-test"

      - name: Install tools
        run: cargo install tokei

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Clippy
        run: |
          EXCLUDES=""
          if [ -f scripts/lint_ignore.txt ]; then
            while IFS= read -r crate; do
              [[ "$crate" =~ ^#.*$ || -z "$crate" ]] && continue
              EXCLUDES="$EXCLUDES --exclude $crate"
            done < scripts/lint_ignore.txt
          fi
          cargo clippy --workspace --all-targets $EXCLUDES -- -D warnings

      - name: Lint checks
        run: bash scripts/tokei_check.sh

      - name: Build
        run: cargo build --workspace

      - name: Run tests
        run: cargo nextest run --workspace

  deny:
    name: Cargo Deny
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true

      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          arguments: --all-features

  security:
    name: Security Audit
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true

      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
